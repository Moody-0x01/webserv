
/*     HOW TO USE:
    try {
        Config config = parse_config_file(argv[1]);
        // config.debug(); // only if needed
    } catch (const std::exception& e) {
        std::cerr << "[" << argv[1] << "] "<< e.what() << "\n";
        return 1;
    }
 */